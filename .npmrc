; Dono runs preact/compat at build time, so the npm-resolved react version is
; cosmetic. @wordpress/components peers react@^18 while other deps pull react@19;
; that tension is harmless here. Install loosely so it doesn't block.
legacy-peer-deps=true
