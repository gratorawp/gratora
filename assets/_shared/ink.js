/**
 * The measurement lives in the design system, beside the preview that paints
 * these colours. Re-exported here so the public bundle, the brand panel and the
 * editor canvas all read one copy of it.
 */

export * from '@gratora/ui/styling/ink';
