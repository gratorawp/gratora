{
  "name": "dono-connect-broker",
  "private": true,
  "version": "0.1.0",
  "description": "Dono Stripe Connect OAuth broker (Cloudflare Worker). Holds the platform secret; the GPL plugin never does.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
